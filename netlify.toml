[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Nuxt SPA (ssr: false): deep links and refreshes must serve the app shell
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
